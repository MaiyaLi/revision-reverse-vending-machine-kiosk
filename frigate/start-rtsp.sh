#!/bin/bash
# This file is no longer needed.
# Frigate uses go2rca with libcamera-vid exec profile (see config.yml)
echo "start-rtsp.sh is deprecated. Frigate handles RTSP streaming via go2rtc in config.yml"
